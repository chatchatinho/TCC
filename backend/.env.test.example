# Copie para .env.test e ajuste se necessário. Usado apenas por `npm test`.
# Aponta para um banco SEPARADO do de desenvolvimento (tests fazem TRUNCATE entre casos).

DATABASE_URL="postgresql://tcc_dev:CHANGE_ME@localhost:5432/tcc_test"
API_PORT=3001
JWT_SECRET="test-only-secret"
CORS_ORIGIN="http://localhost:5173"
FRONTEND_URL="http://localhost:5173"
